```
├── README.md
├── conflic: 规则和模型冲突的数据。下一步是将这个数据交给人标注，变成标注数据。
├── consistent: 规则和模型一致的数据。下一步是和人标注的数据一起合并为最终标注的句子。
├── label: 冲突的被导出后被标注好的数据。下一步是和一致的数据合并，变成最终标注数据。
├── labeled_sent_set: 最终标注的句子。
└── merge: 匹配的结果和模型预测的结果合并后的数据。下一步是用这个数据进行筛选，筛选出冲突的、一致的等。
```